services:
  pdf-parser:
    image: docker.io/jacksonxie/pdf-parser:arm64-local
    container_name: pdf-parser
    volumes:
      # 挂载本地目录到容器，可存放 PDF 输入/输出文件
      # 修改左侧路径为你的本地目录
      - ./tmpdir:/app/data
    restart: unless-stopped
